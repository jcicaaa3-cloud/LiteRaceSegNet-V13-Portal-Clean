# GitHub Pages Link Fix

`index.html` は root に置きます。Pages 設定は以下を推奨します。

```text
Settings -> Pages -> Deploy from a branch -> main -> /root
```

README への raw Markdown link ではなく、repository URL または root の landing page へ誘導します。
